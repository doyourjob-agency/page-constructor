import React from 'react';

import {ContentLayoutBlock} from '../../blocks';
import {ContentLayoutBlockProps} from '../../models';

const ContentLayoutCard = (props: ContentLayoutBlockProps) => <ContentLayoutBlock {...props} />;

export default ContentLayoutCard;
