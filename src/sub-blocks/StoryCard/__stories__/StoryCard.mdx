import {Meta} from '@storybook/addon-docs/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as StoryCardStories from './StoryCard.stories.tsx';

<Meta of={StoryCardStories} />
<StoryTemplate>
## Parameters

`type: "story-card"`

`title?: Title | string` — Title.

`tag?: string` — Tag.

`text?: string` — Right text.

`quote?: string` — Left text.

`logo?: string` — Left logo.

`avatar?: string` — Left avatar.

`autor?: string` — Left autor text.

`background?: string` — Background card.

`data?: {value: string; label?: string}[]` — Center elements.

</StoryTemplate>
