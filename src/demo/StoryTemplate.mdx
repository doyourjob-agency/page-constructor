import {
    Title,
    Subtitle,
    Description,
    Primary,
    Controls,
    Stories,
    Meta,
    Markdown,
} from '@storybook/addon-docs/blocks';

export const StoryTemplate = ({children}) => (
    <>
        <Title />
        <Subtitle />
        {children}
        <Primary />
        <Controls />
        <Stories />
    </>
);
