import {Meta} from '@storybook/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as NewTableStories from './NewTable.stories.tsx';

<Meta of={NewTableStories} />
<StoryTemplate>
## Parameters

`type: 'new-table-block'`

`title?: string` — Title on the left

`description?: string` — Description on the left

`table: NewTable` — NewTable content

-   `content: string[][]` — Text in the table cells

-   `highlighter?: string[]` — Colors in the table rows

-   `customColumnWidth?: ('xxl', 'xl', 'l', 'm', 's', 'xs')[]` — Sizes in the table cells

-   `justify?: ('left' | 'center' | 'right')[]` — Text alignment.

</StoryTemplate>
