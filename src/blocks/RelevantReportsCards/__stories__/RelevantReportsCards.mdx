import {Meta} from '@storybook/addon-docs/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as RelevantReportsCardsStories from './RelevantReportsCards.stories.tsx';

<Meta of={RelevantReportsCardsStories} />
<StoryTemplate>
## Parameters

`type: "relevant-reports-cards-block"`

`typeKey: string;` - A key indicating what data to take from the context.

`title?: Title | string` - Title.

`slider?: boolean` - Slider mode.

`date?: "today" | "week" | "month"` - Date filter.

`dateStart?: string` - Date start filter.

`dateEnd?: string` - Date end filter.

</StoryTemplate>
