import {Meta} from '@storybook/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as LinkTableStories from './LinkTable.stories.tsx';

<Meta of={LinkTableStories} />
<StoryTemplate>
## Parameters

`type: 'link-table-block'`

`title?: Title` — Block title (see [Content blocks](?path=/docs/documentation-types--docs))

`items?: Link[]` — An array with link objects (see [Content blocks](?path=/docs/documentation-types--docs))

</StoryTemplate>
