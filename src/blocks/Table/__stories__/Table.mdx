import {Meta} from '@storybook/addon-docs/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as TableStories from './Table.stories.tsx';

<Meta of={TableStories} />
<StoryTemplate>
## Parameters

`type: 'table-block'`

`title: string` — Title on the left

`titleSize?: 'xs' | 's' | 'm' | 'l' | 'xl'` — Size of the title text. Defaults to `'m'` (same as `display-2` variant).

`description: string` — Description on the left

`buttons?: button[]` — List of Button blocks on the left.

[`links?: Link[]` — An array with link objects on the left.](?path=/docs/documentation-types--docs#link)

`table: Table` — Table content

</StoryTemplate>
