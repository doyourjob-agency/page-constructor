import {Meta} from '@storybook/addon-docs/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as ScrollerStories from './Scroller.stories.tsx';

<Meta of={ScrollerStories} />
<StoryTemplate>
## Parameters

`type: 'scroller-block'`

`title: string` - Title

`text: string` - Text

`widths: string[]` - Width for items

`gapLong: string[]` - Use big space for items

`children:[]` — You can add an array of any available cards here.

`infinite: boolean` — Loop slides by duplicating content (default true)

`scrollSnapCenter: boolean`

`autoScroll: boolean` - use autoScroll and show pagination (default true)

`autoScrollInterval: number` - time in ms (default 3000)

</StoryTemplate>
