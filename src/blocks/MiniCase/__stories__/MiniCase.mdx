import {Meta} from '@storybook/addon-docs/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as MiniCaseStories from './MiniCase.stories.tsx';

<Meta of={MiniCaseStories} />
<StoryTemplate>

## Parameters

`type: "mini-case-block"`

`title?: Title` — Title.

`description?: string` — Description.

`items: string[]` — keys from context data.

</StoryTemplate>
