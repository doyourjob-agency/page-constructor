import {BaseProps} from './../../schema/validators/common';

export const SolutionsBlock = {
    'solutions-block': {
        additionalProperties: false,
        required: [],
        properties: {
            ...BaseProps,
        },
    },
};
