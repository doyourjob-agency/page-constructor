import {Meta} from '@storybook/addon-docs/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as ServicesStories from './Services.stories.tsx';

<Meta of={ServicesStories} />
<StoryTemplate>
## Parameters

`type: 'services-block'`

`title: string` — Title

`serviceLinkType?: 'doc' | 'price'` — Display type

</StoryTemplate>
