import {Meta} from '@storybook/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as RelevantPostsStories from './RelevantPosts.stories.tsx';

<Meta of={RelevantPostsStories} />
<StoryTemplate>
## Parameters

`type: "relevant-posts-block"`

`title?: Title | string` - Title.

`date?: "today" | "week" | "month"` - Date filter.

`dateStart?: string` - Date start filter.

`dateEnd?: string` - Date end filter.

`tags?: string[]` - Tags filter.

`services?: string[]` - Services filter.

`pinnedPost?: boolean` - Pinned post.

</StoryTemplate>
